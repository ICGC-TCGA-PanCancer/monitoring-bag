{
  "rabbitmq": {
    "ssl": {
      "cert_chain_file": "/etc/sensu/ssl/cert.pem",
      "private_key_file": "/etc/sensu/ssl/key.pem"
    },
    "host": "{{ sensu_server_host | default('localhost') }}",
    "port": 5671,
    "vhost": "/sensu",
    "user": "sensu",
    "password": "{{ rabbit_password }}"
  }
}
